{
  "name": "twitch-ad-blocker",
  "version": "4.1.23",
  "description": "Twitch ad blocker with self-healing recovery",
  "private": true,
  "scripts": {
    "sync-docs": "node build/sync-docs.js",
    "build": "node build/build.js",
    "build:minor": "node build/build.js --minor",
    "build:major": "node build/build.js --major",
    "pretest": "node build/sync-docs.js --check",
    "test": "vitest run",
    "test:watch": "vitest",
    "prebuild": "node build/sync-docs.js && npm test"
  },
  "devDependencies": {
    "jsdom": "^27.2.0",
    "nodemon": "^3.0.0",
    "vitest": "^4.0.15"
  }
}