{
  "name": "twitch-stream-healer",
  "version": "4.4.31",
  "description": "Twitch Stream Healer userscript with self-healing recovery",
  "private": true,
  "scripts": {
    "sync-docs": "node build/sync-docs.js",
    "build": "node build/build.js",
    "build:minor": "npm run build -- --minor",
    "build:major": "npm run build -- --major",
    "agent:verify": "node build/agent-verify.js",
    "verify": "npm run agent:verify",
    "manifest:print": "node build/print-manifest.js",
    "pretest": "node build/sync-docs.js --check",
    "test": "vitest run",
    "test:watch": "vitest",
    "prebuild": "node build/sync-docs.js && npm test"
  },
  "devDependencies": {
    "jsdom": "^27.2.0",
    "nodemon": "^3.0.0",
    "vitest": "^4.0.15"
  }
}