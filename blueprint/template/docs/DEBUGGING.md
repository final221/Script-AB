# Debugging

## Baseline
- Run `npm run agent:verify` to execute tests, build, and post-build checks.
- Use generated logs and tag docs for operational triage.

## Checklist
1. Reproduce issue with debug logging enabled.
2. Collect timeline/log output.
3. Validate decision path against `docs/ARCHITECTURE.md`.
